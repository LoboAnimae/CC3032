class A {
    z: Int <- 35;
    a: Int;
    b: Int;
    add(c: Int, d: Int): Int {
        a * c + b * d
    };

    getA(): Int { a };
    getB(): Int { b };
};





class Main {
    e: Int <- 3 + 2 * 4 * 5 + 3 - 2;
    f: Int <- 2;
    g: A <- new A;
    h: Int <- g.add(e, f);
    main(): Int { 1 };
};
