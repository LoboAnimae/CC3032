class Something {
    x: Bool <- "Strrring" < 2;
};

class SomethingElse inherits Something {
    x: Bool <- 1;
    y: Int;
    z: String;
    x(): Bool {{
        WHILE x + 1 < 34 
        LOOP
            x = x + 1
        POOL;
    }};
};

class Main {
    x: Int <- 1;
    main(): Void { self };
};