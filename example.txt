class A {
    a: Int;
    b: Int;
    add(c: Int, d: Int): Int {
        a * c + b * d
    };

    getA(): Int { a };
    getB(): Int { b };
    setA(newA: Int): Bool {
        {a <- newA; true;}
    };
};


class Main {
    e: Int <- 34 + 2 * 5 - 3;
    f: Int <- 2;
    g: A <- new A;
    h: Int <- 2;
    main(): Int {
        {f <- g.add(e, f); 1;}
    };
};